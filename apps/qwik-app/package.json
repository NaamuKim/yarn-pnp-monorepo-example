{
  "name": "@mono/qwik-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --mode ssr --port 3010",
    "build": "qwik build",
    "build.client": "vite build",
    "build.ssr": "vite build --ssr src/entry.ssr.tsx",
    "preview": "qwik build preview && vite preview",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist server"
  },
  "dependencies": {
    "@builder.io/qwik": "^1.9.0",
    "@builder.io/qwik-city": "^1.9.0",
    "@mono/api-client": "workspace:*",
    "@mono/ui": "workspace:*",
    "@mono/utils": "workspace:*",
    "undici": "^6.19.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.4.0"
  }
}
